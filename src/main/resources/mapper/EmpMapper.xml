<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.realman.SchoolSystem.mapper.EmpMapper">

    <!-- Reusable where clause -->
    <sql id="empFilter">
        <where>
            <if test="param.name != null and param.name != ''">
                e.name like concat('%', #{param.name}, '%')
            </if>
            <if test="param.gender != null">
                and e.gender = #{param.gender}
            </if>
            <if test="param.begin != null and param.end != null">
                and e.entry_date between #{param.begin} and #{param.end}
            </if>
        </where>
    </sql>

    <!-- Count query using the same filters -->
    <select id="count" resultType="java.lang.Long">
        select count(*)
        from emp e
        left join dept d on e.dept_id = d.id
        <include refid="empFilter"/>
    </select>

    <!-- List query -->
    <select id="list" resultType="com.realman.SchoolSystem.pojo.Emp">
        select e.*, d.name as deptName
        from emp e
        left join dept d on e.dept_id = d.id
        <include refid="empFilter"/>
        order by e.update_time desc
        limit #{offset}, #{param.pageSize}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="emp.id">
        INSERT INTO emp (
        username,
        name,
        gender,
        phone,
        job,
        salary,
        image,
        entry_date,
        dept_id,
        create_time,
        update_time
        ) VALUES (
        #{emp.username},
        #{emp.name},
        #{emp.gender},
        #{emp.phone},
        #{emp.job},
        #{emp.salary},
        #{emp.image},
        #{emp.entryDate},
        #{emp.deptId},
        #{emp.createTime},
        #{emp.updateTime}
        )
    </insert>

    <delete id="deleteByIds">
        delete from emp
        where id in
        <foreach collection="ids" item="empId" open="(" separator="," close=")">
            #{empId}
        </foreach>
    </delete>

<!--    use this because emp is a complex  object, we have to manually map the fields-->
    <resultMap id="empResultMap" type="com.realman.SchoolSystem.pojo.Emp">
        <!-- Emp fields -->
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="name" column="name"/>
        <result property="gender" column="gender"/>
        <result property="phone" column="phone"/>
        <result property="job" column="job"/>
        <result property="salary" column="salary"/>
        <result property="image" column="image"/>
        <result property="entryDate" column="entry_date"/>
        <result property="deptId" column="dept_id"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>

        <!-- emp_expr list -->
        <collection property="exprList" ofType="com.realman.SchoolSystem.pojo.EmpExpr">
            <id property="id" column="ee_id"/>
            <result property="empId" column="ee_empid"/>
            <result property="begin" column="ee_begin"/>
            <result property="end" column="ee_end"/>
            <result property="company" column="ee_company"/>
            <result property="job" column="ee_job"/>
        </collection>
    </resultMap>

    <select id="getById" resultMap="empResultMap">
        select e.*,
        ee.id ee_id,
        ee.emp_id ee_empid,
        ee.begin ee_begin,
        ee.end ee_end,
        ee.company ee_company,
        ee.job ee_job
        from emp e
        left join emp_expr ee on e.id = ee.emp_id
        where e.id= #{id}
    </select>

    <update id="updateById">
        update emp
        <set>
            <if test="username != null and username !=''">username = #{username},</if>
            <if test="password != null and password !=''">password = #{password},</if>
            <if test="name != null and name !=''">name = #{name},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="phone != null and phone!=''">phone = #{phone},</if>
            <if test="job != null">job = #{job},</if>
            <if test="salary != null">salary = #{salary},</if>
            <if test="image != null and image!=''">image = #{image},</if>
            <if test="entryDate != null">entry_date = #{entryDate},</if>
            <if test="deptId != null">dept_id = #{deptId},</if>
            <if test="updateTime != null">update_time = #{updateTime}</if>
            <!-- always update -->
        </set>
        where id = #{id};
    </update>

    <select id="countEmpJobData" resultType="com.realman.SchoolSystem.pojo.JobStatics">
        SELECT
        (CASE
        WHEN job = 1 THEN 'Homeroom Teacher'
        WHEN job = 2 THEN 'Lecturer'
        WHEN job = 3 THEN 'Student Affairs Supervisor'
        WHEN job = 4 THEN 'Teaching and Research Supervisor'
        WHEN job = 5 THEN 'Consultant'
        ELSE 'Other'
        END) AS job_name,
        COUNT(*) AS total
        FROM emp
        GROUP BY job_name
        ORDER BY total;
    </select>
</mapper>


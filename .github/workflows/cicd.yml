#name: CI/CD Deploy
#
#on:
#  push:
#    branches:
#      - main  # 你想自动部署的分支
#
#jobs:
#  build-and-deploy:
#    runs-on: ubuntu-latest
#
#    steps:
#      - name: Checkout code
#        uses: actions/checkout@v3
#
#      - name: Set up Docker Buildx
#        uses: docker/setup-buildx-action@v2
#
#      - name: Login to DockerHub
#        uses: docker/login-action@v2
#        with:
#          username: ${{ secrets.DOCKER_USERNAME }}
#          password: ${{ secrets.DOCKER_PASSWORD }}
#
#      - name: Build Docker image
#        run: docker build -t yourdockerhubusername/schoolsystem:latest .
#
#      - name: Push Docker image
#        run: docker push yourdockerhubusername/schoolsystem:latest
#
#      - name: SSH and deploy
#        uses: appleboy/ssh-action@v0.1.7
#        with:
#          host: ${{ secrets.SERVER_IP }}
#          username: ${{ secrets.SERVER_USER }}
#          key: ${{ secrets.SERVER_SSH_KEY }}
#          script: |
#            docker pull yourdockerhubusername/schoolsystem:latest
#            docker stop schoolsystem-server || true
#            docker rm schoolsystem-server || true
#            docker run -d --name schoolsystem-server -p 8080:8080 --network mynetwork yourdockerhubusername/schoolsystem:latest
